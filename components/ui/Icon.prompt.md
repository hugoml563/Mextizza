The single icon surface for the whole system. Stroke only, `currentColor`, 20/24px.

```jsx
<Icon name="cart" />
<Icon name="clock" size={16} color="var(--text-muted)" />
```

If Mextizza adopts a real icon set, replace `PATHS` in `Icon.jsx` and nothing else changes.
