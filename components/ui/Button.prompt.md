The ordering CTA. An intentional addition — the sources have no button, so this one is built purely from the foundations.

```jsx
<Button tone="primary" size="lg" icon="cart">Agregar al pedido</Button>
<Button tone="dark" icon="whatsapp">Pedir por WhatsApp</Button>
<Button tone="outline" size="sm">Ver menú</Button>
```

Copy is uppercase, verb-first, and short: "Agregar al pedido", "Confirmar", "Pedir por WhatsApp".
