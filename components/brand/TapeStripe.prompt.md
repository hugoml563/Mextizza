The sealing-tape stripe. Use as an edge or a divider — never as a large background field.

```jsx
<div style={{ position: 'relative', border: 'var(--border-frame)', padding: 'var(--pad-frame)' }}>
  <TapeStripe position="top" />
  …
</div>
```
