Every Mextizza surface is one of these three. Reach for it instead of writing a new card.

```jsx
<FramedPanel variant="object" tape="top">
  <Stamp lines={['Hecho a', 'mano', 'en 48h']} />
</FramedPanel>
<FramedPanel variant="hero"><Wordmark showSubtitle /></FramedPanel>
```
